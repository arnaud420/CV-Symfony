{% extends 'base.html.twig' %}

{% block body %}
    <div id="wrapper">
        blog

        <h1>{{ category.name }}</h1>

        {% for post in posts %}
            <a href="{{ path('post_show', {'post_id': post.id}) }}">{{ post.title }}</a>
        {% endfor %}
    </div>
{% endblock %}